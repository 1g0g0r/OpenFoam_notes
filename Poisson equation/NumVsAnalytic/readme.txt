Данный модуль содержит реализацию пост-процессинга сравнения    
численного решения с аналитическим уравнения Пуассона с правой частью     
f = cos(x * t) * cos(y * t) + 1.5 * sin(t) * sin(t) * x * x * y * y;   

Для сборки следует вызвать команду wmake libso
